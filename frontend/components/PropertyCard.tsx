import Link from 'next/link';import {BedDouble,MapPin,Maximize,ArrowUpRight} from 'lucide-react';import type {Property} from '@/lib/types';
function price(p:number,type:string){return type?.toLowerCase()==='rent'?`₹${p.toLocaleString('en-IN')}/mo`:`₹${(p/10000000).toFixed(2)} Cr`}
export default function PropertyCard({property}:{property:Property}){const image=property.images?.[0]?.image_url;return <Link href={`/properties/${property.id}`} className="group block overflow-hidden rounded-[28px] border border-[#e8e5dc] bg-[#fffdf8] transition hover:-translate-y-1 hover:shadow-soft"><div className="relative aspect-[1.28] overflow-hidden bg-[#dfe5dc]">{image?<img src={image} alt={property.title} className="h-full w-full object-cover transition duration-700 group-hover:scale-105"/>:<div className="h-full w-full bg-gradient-to-br from-[#cbd9c8] via-[#e7e4d8] to-[#b8c6b3]"/>}<div className="absolute left-4 top-4 rounded-full bg-white/90 px-3 py-1.5 text-xs font-bold capitalize">{property.listing_type}</div><div className="absolute bottom-4 right-4 grid h-11 w-11 place-items-center rounded-full bg-[#d9ef73] text-[#173d2c] transition group-hover:rotate-45"><ArrowUpRight size={19}/></div></div><div className="p-5"><div className="mb-2 flex items-start justify-between gap-3"><h3 className="line-clamp-2 font-bold leading-tight">{property.title}</h3><span className="whitespace-nowrap text-lg font-bold text-[#173d2c]">{price(property.price,property.listing_type)}</span></div><p className="mb-4 flex items-center gap-1 text-sm text-[#68736d]"><MapPin size={15}/>{property.location}</p><div className="flex gap-4 border-t border-[#e8e5dc] pt-4 text-xs font-semibold text-[#68736d]">{property.bedrooms!=null&&<span className="flex items-center gap-1"><BedDouble size={15}/>{property.bedrooms} beds</span>}{property.bathrooms!=null&&<span>{property.bathrooms} baths</span>}{property.area_sqft!=null&&<span className="flex items-center gap-1"><Maximize size={14}/>{property.area_sqft.toLocaleString()} sqft</span>}</div></div></Link>}
