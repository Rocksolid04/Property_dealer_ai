'use client';import Link from 'next/link';import {useState} from 'react';import {registerUser} from '@/lib/api';export default function Register(){const[name,setName]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');async function submit(e:React.FormEvent){e.preventDefault();try{await registerUser({name,email,password});setMsg('Account created. You can now sign in.')}catch{setMsg('Registration failed. Please check your details.')}}return <main className="container flex min-h-[calc(100vh-73px)] items-center justify-center py-14"><form onSubmit={submit} className="w-full max-w-md rounded-[32px] border border-[#e8e5dc] bg-white p-8 shadow-soft"><p className="mb-3 text-sm font-bold uppercase tracking-[.2em] text-[#648b46]">Join EstateAI</p><h1 className="serif text-5xl">Create your account.</h1><label className="mt-8 block text-sm font-bold">Name<input value={name} onChange={e=>setName(e.target.value)} required className="mt-2 w-full rounded-2xl border border-[#e8e5dc] p-4"/></label><label className="mt-5 block text-sm font-bold">Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required className="mt-2 w-full rounded-2xl border border-[#e8e5dc] p-4"/></label><label className="mt-5 block text-sm font-bold">Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" required className="mt-2 w-full rounded-2xl border border-[#e8e5dc] p-4"/></label><button className="mt-7 w-full rounded-2xl bg-[#173d2c] py-4 font-bold text-white">Create account</button>{msg&&<p className="mt-4 text-sm text-[#648b46]">{msg}</p>}<p className="mt-7 text-center text-sm text-[#68736d]">Already have an account? <Link href="/login" className="font-bold text-[#173d2c]">Sign in</Link></p></form></main>}
