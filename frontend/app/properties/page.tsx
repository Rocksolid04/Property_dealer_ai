'use client';import {useEffect,useState} from 'react';import {Search,SlidersHorizontal} from 'lucide-react';import {getProperties} from '@/lib/api';import type {Property} from '@/lib/types';import PropertyCard from '@/components/PropertyCard';
export default function Properties(){const[p,setP]=useState<Property[]>([]);const[q,setQ]=useState('');useEffect(()=>{getProperties('?limit=50').then(setP).catch(()=>{})},[]);const shown=p.filter(x=>`${x.title} ${x.location} ${x.property_type}`.toLowerCase().includes(q.toLowerCase()));return <main className="container py-14"><div className="mb-12"><p className="mb-3 text-sm font-bold uppercase tracking-[.2em] text-[#648b46]">Explore</p><h1 className="serif text-6xl">Find your next place.</h1><p className="mt-4 max-w-2xl text-[#68736d]">Browse the live property inventory connected to your FastAPI backend.</p></div><div className="mb-8 flex gap-3 rounded-2xl border border-[#e8e5dc] bg-white p-2"><Search className="m-3 text-[#68736d]"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search Mumbai, Worli, apartment..." className="flex-1 outline-none"/><button className="hidden rounded-xl bg-[#173d2c] px-5 text-white sm:flex sm:items-center sm:gap-2"><SlidersHorizontal size={17}/> Filters</button></div>{shown.length?<div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{shown.map(x=><PropertyCard key={x.id} property={x}/>)}</div>:<div className="rounded-3xl bg-white p-16 text-center">No properties found.</div>}</main>}
