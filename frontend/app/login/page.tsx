'use client';import Link from 'next/link';import {useState} from 'react';import {loginUser} from '@/lib/api';export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');async function submit(e:React.FormEvent){e.preventDefault();try{const r=await loginUser(email,password);localStorage.setItem('access_token',r.access_token);setMsg('Login successful.')}catch{setMsg('Login failed. Check your email and password.')}}return <main className="container grid min-h-[calc(100vh-73px)] items-center py-14 lg:grid-cols-2"><div className="hidden lg:block"><p className="mb-4 text-sm font-bold uppercase tracking-[.2em] text-[#648b46]">Welcome back</p><h1 className="serif text-7xl">Your next<br/><em>address awaits.</em></h1></div><form onSubmit={submit} className="mx-auto w-full max-w-md rounded-[32px] border border-[#e8e5dc] bg-white p-8 shadow-soft"><h2 className="serif text-4xl">Sign in</h2><p className="mt-2 text-sm text-[#68736d]">Access your EstateAI account.</p><label className="mt-8 block text-sm font-bold">Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required className="mt-2 w-full rounded-2xl border border-[#e8e5dc] p-4 outline-none focus:border-[#648b46]"/></label><label className="mt-5 block text-sm font-bold">Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" required className="mt-2 w-full rounded-2xl border border-[#e8e5dc] p-4 outline-none focus:border-[#648b46]"/></label><button className="mt-7 w-full rounded-2xl bg-[#173d2c] py-4 font-bold text-white">Sign in</button>{msg&&<p className="mt-4 text-sm text-[#648b46]">{msg}</p>}<p className="mt-7 text-center text-sm text-[#68736d]">New here? <Link href="/register" className="font-bold text-[#173d2c]">Create an account</Link></p></form></main>}
