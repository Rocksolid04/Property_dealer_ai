[project]
name = "flask-frontend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "aditya singh", email = "adityaainghmar04@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
flask-frontend = "flask_frontend:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
